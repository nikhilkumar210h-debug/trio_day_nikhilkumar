# Functions folder (unused)

Push notifications moved to **OneSignal** + Cloudflare Worker:

- Client: onesignal.js / 
otifications.js
- Worker: workers/send-push.js
- Config: onesignal-config.js (PUSH_API_URL)

Do not deploy this folder.
